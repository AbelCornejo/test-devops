version: 0.2
phases:
  install:
    commands:
      - echo "CODEBUILD_SRC_DIR = $CODEBUILD_SRC_DIR"
      - ls -l $CODEBUILD_SRC_DIR
      - echo "Iniciando Test"
  build:
    commands:
      - echo "Verificando archivos de la web..."
      - test -f index.html || exit 1
      - echo "Test completado exitosamente"
artifacts:
  files:
    - '**/*'

