version: 0.2

phases:
  install:
    commands:
      - echo "Instalando dependencias si fueran necesarias"
  build:
    commands:
      - echo "Compilando la aplicación"
      - echo "Copiando archivos al directorio de salida"

artifacts:
  files:
    - '**/*'

